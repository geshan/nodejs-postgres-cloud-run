{
    "name": "nodejs-postgres-cloud-run",
    "hooks": {
        "prebuild": {
            "commands": [
                "gcloud auth configure-docker --quiet --verbosity=error"
            ]
        }
    }
}
